import React from "react";
import { Icon } from "@chakra-ui/react";
import { MdArchive } from "react-icons/md";

const ArchiveIcon = (props) => {
  return <Icon as={MdArchive} {...props} />;
};

export default ArchiveIcon;
